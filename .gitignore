# Core OS / Editor
.DS_Store
Thumbs.db
Icon?
*.swp
*.swo
*~

# Node (if later adding tooling/tests)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json
.yarn-integrity

# Coverage / Reports
coverage/
.nyc_output/

# Build artifacts (if bundlers introduced later)
dist/
build/
.out/
.tmp/
.cache/
.vite/
.next/

# Temp / Scratch
.tmp*/
*.tmp
*.log
logs/

# Environment / secrets (should not exist in precourse exercises)
.env
.env.*
!.env.example

# IDE / Tooling
.vscode/
.idea/
*.iml

# System generated
ehthumbs.db
Desktop.ini

# Mac metadata
.AppleDouble
.LSOverride

# Archives / Packages
*.zip
*.tar
*.tar.gz
*.tgz
*.rar

# Images source working files (keep final exported assets only)
*.psd
*.ai
*.sketch
*.xcf

# Python (if any optional exercises later)
__pycache__/
*.pyc
*.pyo
*.pyd
.mypy_cache/
.pytest_cache/

# SASS / PostCSS intermediate (future)
*.css.map
*.sass-cache/

# Optional lockfiles (decide to commit later if reproducibility needed)
# Remove from ignore if you want deterministic installs.
# package-lock.json

# Git merge / patch backups
*.orig
*.rej

# Misc
.sass-cache
npm-debug.log

.github/copilot-instructions.md

# End of .gitignore
